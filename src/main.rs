use std::{ptr::swap, collections::HashSet};

fn main() {
    println!("Hello, world!");
    // let mut nums: Vec<i32> = Vec::with_capacity(5);
    // nums.push(1);
    // nums.push(3);
    // nums.push(5);
    // println!("the nums: {:?}", nums);
    // let mut nums_s: &[i32] = &nums;
    // if &nums_s[1] != &nums_s[2] {
    //     swap(&mut nums_s[1], &mut nums_s[2]);
    // }

}
