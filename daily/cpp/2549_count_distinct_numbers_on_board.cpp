#include <bits/stdc++.h>
using namespace std;

class Solution {
  /**
   * @brief count distinct numbers on board
   *
   *
   * @param n
   * @return int
   */
  int distinctIntegers(int n) { return n == 1 ? 1 : n - 1; }
};